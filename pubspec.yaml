name: outdoor_companion
description: 户外伴行 - 为三种车轮，提供一种确定性
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.3.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  url_launcher: ^6.2.0
  provider: ^6.1.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
